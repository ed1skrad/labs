#include<stdio.h>  

int main()
{
	double epsilon = 0.0001; //????????
	int x; //????? ????? ??? ???????? ???????? ?? ??????? ???????  
	printf("Input sin in radians:");
	scanf_s("%d", &x);
	double member_of_row = 2, sinx, n = 1; //n ??????????? ????? ?? ???????????? ???? ?????  
	sinx = x;
	double numerator = x, denumerator = 1.0; //????????? ? ???????????  
	for (; member_of_row >= epsilon && n <= 100;)
	{
		numerator *= -x * x; //?????????? ?????????
		n++;
		denumerator *= n;//?????????? ???????????
		n++;
		denumerator *= n;
		member_of_row = numerator / denumerator;
		sinx += member_of_row;
		if (member_of_row < 0)
		{
			member_of_row *= -1;
		}
		else continue;
	}

	epsilon = 0.0001; //????????
	 member_of_row = 2, n = 0; //n ??????????? ????? ?? ???????????? ???? ?????  
	double cosx = 1;
	 numerator = 1, denumerator = 1; //????????? ? ???????????  
	for (; member_of_row >= epsilon && n <= 100;)
	{
		numerator *= (-x * x);
		n++;
		denumerator *= n;
		n++;
		denumerator *= n;
		member_of_row = numerator / denumerator;
		cosx += member_of_row;
		if (member_of_row < 0)
		{
			member_of_row *= -1;
		}
		else continue;
	}
	double tgx = sinx / cosx;

	printf("%F", tgx);
	return 0;
	
}
